# Architecture decisions

Status: synthesis of the product conversation, 2026-09-14. “Settled” below means
the direction expressed by that source and the current request, not approval of
every detail in a previous assistant-generated draft.

## Settled product direction

| Decision                            | Consequence                                                                    |
| ----------------------------------- | ------------------------------------------------------------------------------ |
| One Woof product                    | Both plugins and the SDK share concepts; orchestration is part of Woof.        |
| SDK owns the engine                 | Gates, retries, validation and run state are reusable outside a plugin UI.     |
| Herdr remains runtime               | Reuse its agent/session/pane/worktree operations and integration capabilities. |
| Agent callers come first            | Rich structured input is primary; manual CLI ergonomics are secondary.         |
| Identity differs from stage         | Repair can reuse the builder; stage changes do not imply new sessions.         |
| Artifacts carry substantive work    | Review files are required; envelopes carry control data and references.        |
| Engine validates results            | Idle state and a completion claim cannot by themselves satisfy a stage.        |
| Every loop is bounded               | Work retries, format repairs, rounds and waits have explicit policies.         |
| Configuration has two scopes        | User defaults and project overrides work independently of Claude Code.         |
| Observability is part of the engine | Snapshots and updates support multiple consumers from the start.               |
| MCP is optional                     | Core invocation and result submission have a non-MCP path.                     |
| TUI later                           | Establish the observation contract now; defer the renderer.                    |

## Superseded recommendations

| Earlier idea                                                | Current resolution                                                                                                |
| ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Woof is only developer UX; Horde owns loops                 | Woof is the product and `HerdrAgentsSDK` contains orchestration.                                                  |
| Review findings live in a rich JSON response; file optional | The review artifact is canonical and required; JSON is the control envelope.                                      |
| Worker results only through MCP `woof_submit`               | Keep submission validation and idempotence; remove the transport requirement.                                     |
| Reject artifact/file protocols altogether                   | Evaluate publication and acknowledgement semantics; do not reject the user's required artifacts.                  |
| Stashed Claude-only workers and automatic permission bypass | These are implementation proposals, not requirements; capability and permission policies need explicit decisions. |
| Fixed multi-package layout, YAML or fluent workflow API     | Examples illustrate possible shapes; none is a settled packaging or syntax contract.                              |

## Open implementation decisions

| Choice                 | Status                                                          | Recommendation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Result submission      | **Prototyped (p1)**                                             | Artifact publication plus a worker-callable CLI bridge into the SDK (D1).                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Produced: `test/submit.cli.test.ts`, `test/journal.process.test.ts` (real-process tests: malformed, partial, duplicate and stale submissions); live worker completion in `docs/research/result-handoff-live.log`; commit `d1c3ee4`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Run hosting            | Open                                                            | Evaluate one process per run in a Herdr-managed pane.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Needed: readiness, cancellation, caller exit, runtime death and two concurrent runs. Unchanged by p2: every snapshot reports `liveness.owner: "unhosted"`, because there is still no run-owner process to be reachable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Persistence and resume | Open — journal exists, resume not claimed                       | Keep inspectable state/artifacts; specify crash behavior before promising resume.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Produced: the append-only `journal.jsonl` (`src/journal/`) now also carries run facts (`agent.assigned`, `request.dispatched`, `run.terminated`, an optional `plan` on `run.opened`) and derives attempt/acceptance/run state by replay (`src/state/reducer.ts`). The lock-release compare-and-remove race (C1) is unchanged in kind and still not fixed (Node 22 has no `flock`/`fcntl` without a new dependency); p2 adds fail-closed evidence instead: `test/lock-race.process.test.ts` simulates two writers racing the same journal and shows `readJournal`, `readSnapshot`, `woof run show` and `woof submit` all report `journal_corrupt` afterward rather than a silently mixed journal. Needed: lost-owner detection and no duplicate effects after any supported recovery — crash resume is explicitly not claimed. |
| Module/package layout  | Open — a p2 layout is enforced                                  | Directories with a one-way import rule (`domain/` → `contracts/`; `runtime/` → `domain/`, `contracts/`; `journal/` → `contracts/`, `domain/`, `state/reducer`; `state/` → `domain/`, `contracts/`, `journal/`; `observe/` → `state/`, `journal/`, `contracts/`; nothing imports `cli.ts`), enforced by a process test that scans compiled `dist/**/*.js` import specifiers (D8). A `herdr-woof/testing` subpath export ships `createScriptedRuntime` outside the production entry, so a test double is never mistaken for a supported runtime. | Produced: `test/boundaries.process.test.ts` (also asserts `test/unit/**` imports only `dist/`, never `../src`). Needed: independently usable SDK and no plugin/UI dependency in the engine; a second real consumer before any workspace-package split.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Workflow syntax        | Open                                                            | Use TypeScript first if the existing loader remains suitable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Needed: two complete workflows and real runtime loader tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Configuration          | Open                                                            | Extend `.woof/` and `~/.woof/` precedent with documented precedence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Needed: nested project/worktree discovery, provenance, validation and override cases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Worker providers       | Open — a capability boundary shipped, one provider live-checked | The `RuntimeAdapter` interface (`openPane, startAgent, observe, waitFor, deliver, stop`) is the capability boundary (D1); `createHerdrCliRuntime` implements it over the Herdr CLI (`herdr agent`/`pane` JSON commands, never terminal reads or key sends), and `createScriptedRuntime` implements it in memory for tests (`herdr-woof/testing`).                                                                                                                                                                                              | Produced: `test/herdr-adapter.process.test.ts` (fake-`herdr` fixture, real child process, spawn boundary and pane-ownership rules); live acceptance against a real `claude` agent through Herdr — probe plus 11/11 hard gates passing (`docs/research/runtime-adapter-live.log`, regenerated fresh against the HEAD it documents each verification round). Needed: a second provider kind beyond `claude` to prove the boundary generalizes.                                                                                                                                                                                                                                                                                                                                                                                  |
| Artifact contract      | Open — p1 subset prototyped, p2 adds opt-in detection           | Immutable accepted versions with small parseable control metadata. Same-user write access to `accepted/` is unchanged in p2 (C2): `chmod`/`chflags` were rejected as friction, not a boundary, so the response is detection, not prevention.                                                                                                                                                                                                                                                                                                   | Produced: the p1 subset — immutable accepted copy under `accepted/<stage>/visit-<n>/attempt-<m>/`, scoped to that attempt's own artifact directory (`test/submit.cli.test.ts`, `test/engine-paths.cli.test.ts`). p2 adds opt-in re-hashing: `readSnapshot(runDir, {verifyArtifacts: true})` / `woof run show --verify-artifacts` report `integrity.artifacts = {checked, altered}` via `acceptedCopyProblem` (`src/journal/accepted-copy.ts`). Needed: metadata/verdict agreement between the artifact and the envelope, per-stage structural schemas, and — carried into phase 3 as the C2 requirement — a downstream consumer that verifies an artifact before use, are not implemented.                                                                                                                                    |
| Runtime observation    | **Prototyped (p2)**                                             | Pull-based Herdr CLI adapter, no socket client (D1); runtime lifecycle stays an in-memory, freshness-stamped overlay over the snapshot, never a journal record (D3) — a Herdr sample is lossy by construction, and journaling every poll would make replay depend on sampling cadence and wall clock.                                                                                                                                                                                                                                          | Produced: `test/herdr-adapter.process.test.ts` (spawn boundary, pane ownership, delivery-precondition mapping, all through a fake `herdr` fixture reached by absolute path, never PATH); live acceptance against a real `claude` agent through Herdr, probe plus 11/11 hard gates passing, `stateChangeSeq` confirmed monotonic and server-global across two verification rounds (`docs/research/runtime-adapter-live.log`). Needed: a second runtime kind, and independent confirmation of R1/R2/R4 (whether `agent prompt --wait` and `agent wait` behave exactly as inferred from help text) beyond the live gates already run.                                                                                                                                                                                            |

Resolve these into a spec and bounded tickets. Reopen product direction only when
new evidence shows a real conflict. Avoid repeating the entire design discussion
to choose a filename or helper function.

## Sources

The [conversation](https://chatgpt.com/c/6aa71569-4e7c-83ed-b6d6-03992919a7c4)
provides the direction and final artifact correction. The
[project assessment](../research/project-assessment.md) identifies the stashed
draft and scaffold branch as separate historical inputs.
