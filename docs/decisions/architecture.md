# Architecture decisions

Status: synthesis of the product conversation, 2026-09-14. “Settled” below means
the direction expressed by that source and the current request, not approval of
every detail in a previous assistant-generated draft.

## Settled product direction

| Decision                            | Consequence                                                                    |
| ----------------------------------- | ------------------------------------------------------------------------------ |
| One Woof product                    | Both plugins and the SDK share concepts; orchestration is part of Woof.        |
| SDK owns the engine                 | Gates, retries, validation and run state are reusable outside a plugin UI.     |
| Herdr remains runtime               | Reuse its agent/session/pane/worktree operations and integration capabilities. |
| Agent callers come first            | Rich structured input is primary; manual CLI ergonomics are secondary.         |
| Identity differs from stage         | Repair can reuse the builder; stage changes do not imply new sessions.         |
| Artifacts carry substantive work    | Review files are required; envelopes carry control data and references.        |
| Engine validates results            | Idle state and a completion claim cannot by themselves satisfy a stage.        |
| Every loop is bounded               | Work retries, format repairs, rounds and waits have explicit policies.         |
| Configuration has two scopes        | User defaults and project overrides work independently of Claude Code.         |
| Observability is part of the engine | Snapshots and updates support multiple consumers from the start.               |
| MCP is optional                     | Core invocation and result submission have a non-MCP path.                     |
| TUI later                           | Establish the observation contract now; defer the renderer.                    |

## Superseded recommendations

| Earlier idea                                                | Current resolution                                                                                                |
| ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Woof is only developer UX; Horde owns loops                 | Woof is the product and `HerdrAgentsSDK` contains orchestration.                                                  |
| Review findings live in a rich JSON response; file optional | The review artifact is canonical and required; JSON is the control envelope.                                      |
| Worker results only through MCP `woof_submit`               | Keep submission validation and idempotence; remove the transport requirement.                                     |
| Reject artifact/file protocols altogether                   | Evaluate publication and acknowledgement semantics; do not reject the user's required artifacts.                  |
| Stashed Claude-only workers and automatic permission bypass | These are implementation proposals, not requirements; capability and permission policies need explicit decisions. |
| Fixed multi-package layout, YAML or fluent workflow API     | Examples illustrate possible shapes; none is a settled packaging or syntax contract.                              |

## Open implementation decisions

| Choice                 | Status                                                                                             | Recommendation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------- | -------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Result submission      | **Prototyped (p1)**                                                                                | Artifact publication plus a worker-callable CLI bridge into the SDK (D1).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Produced: `test/submit.cli.test.ts`, `test/journal.process.test.ts` (real-process tests: malformed, partial, duplicate and stale submissions); live worker completion in `docs/research/result-handoff-live.log`; commit `d1c3ee4`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Run hosting            | **Prototyped (p4), live-verified**                                                                 | One scheduler process per run in a Herdr pane, claimed exclusively with a heartbeat (D1/D2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Produced: `woof run start`/`woof run host`, `src/host/{claim,probe,launch,run,metadata}.ts`, `test/run-start.cli.test.ts`, `test/host.process.test.ts`; live acceptance: a hosted run surviving the caller's exit, a SIGKILLed host reported `lost` then cancelled via `woof run cancel`, and Herdr-action-driven start/status/cancel against the correct focused project, all confirmed against real Herdr and Claude Code (`docs/research/product-integration-live.log`). `--host foreground`/`woof run build-review` still run the scheduler as a foreground process with no owner (unchanged: `liveness.owner: "unhosted"`). Needed: crash resume and re-hosting a lost run (explicit non-goals, not claimed); a stress case beyond the two concurrent runs already exercised live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Persistence and resume | Open — journal exists, resume not claimed                                                          | Keep inspectable state/artifacts; specify crash behavior before promising resume.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Produced: the append-only `journal.jsonl` (`src/journal/`) carries run facts (`agent.assigned`, `request.dispatched`, `run.terminated`, an optional `plan` on `run.opened`) and, since p3, four scheduler-written record types (`gate.recorded`, `run.blocked`, `run.unblocked`, `delivery.reconciled`, `src/journal/control-records.ts`), all replayed by `src/state/reducer.ts`. The lock-release compare-and-remove race (C1) is unchanged in kind, a fourth time: p4's pane host is one scheduler process, not one journal writer — workers still append through their own `woof submit` processes and `woof run cancel` through the same `withJournalLock`. Liveness adds diagnosis, not prevention: once `woof status` reports the owner `lost`, a `run cancel` against a stale `journal.lock` waits 5 s and exits 3 `journal_busy`; the operator deletes `<runDir>/journal.lock` by hand and runs `run cancel` again — Woof never removes a lock itself. Needed: lost-owner detection and no duplicate effects after any supported recovery — crash resume is explicitly not claimed.                                                                                                                                                                                                                                                                    |
| Module/package layout  | Open — first half satisfied (p5)                                                                   | Directories with a one-way import rule (`domain/` → `contracts/`; `runtime/` → `domain/`, `contracts/`; `journal/` → `contracts/`, `domain/`, `state/reducer`; `state/` → `domain/`, `contracts/`, `journal/`; `observe/` → `state/`, `journal/`, `contracts/`; nothing imports `cli.ts`), enforced by a process test that scans compiled `dist/**/*.js` import specifiers (D8). p3 adds `scheduler/` (→ `scheduler`, `domain`, `contracts`, `state`, `runtime`, `submission/attempt.js`, `journal/accepted-copy.js`; never `journal/journal.js` or `observe/`) and `workflows/` (→ `workflows`, `scheduler` types/helpers, `domain`, `contracts`); only `index.ts`, `cli.ts` and the two areas themselves import either. A `herdr-woof/testing` subpath export ships `createScriptedRuntime` outside the production entry, so a test double is never mistaken for a supported runtime; the built-in workflow stage names never appear as string literals in `dist/scheduler/**`. | Produced: `test/boundaries.process.test.ts` (also asserts `test/unit/**` imports only `dist/`, never `../src`, and greps `dist/scheduler` for `appendRecord` and the built-in stage literals). **The first half of "Needed" is satisfied (p5):** the same boundaries test proves the engine imports no plugin/UI module, and `test/sdk.process.test.ts` runs a full workflow (`runWorkflow`) through the built package with no Woof UI involved (the Optional-adapters acceptance row, `v1-evidence.md`). Still needed: a second real consumer before any workspace-package split.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Workflow syntax        | **Prototyped (p3), generalized (p5)**                                                              | A workflow definition is a strip-only TypeScript (or compiled `.js`/`.mjs`) ES module with a default export and a static `edges` table, loaded with `import(pathToFileURL(path))`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Produced: the loader probe — the same `.ts` module loads on Node 22.18.0 and 26.7.0 via native type stripping; `enum` fails on both with `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`, mapped to `definition_syntax_unsupported`. `test/workflow-loader.process.test.ts` (real child `node`, run once under `npx -y node@22.18.0` in addition to the repository's declared Node) covers a valid `.ts`/`.mjs` definition, `enum.ts`, a missing default export, a malformed shape, a throwing module body and a missing path. `src/scheduler/definition.ts`, `src/scheduler/loader.ts`. **"Needed: two complete workflows" is satisfied (p5):** a second built-in (`plan-build-review`) and one external, project-authored definition (`scribe`) both admit and run to completion with no engine file changed for the definition contract itself; `validateWorkflowDefinition(planBuildReviewWorkflow)` is `ok` and `decide()` grew no branch. Live-verified: `docs/research/plan-build-review-live.log` (13/13) and `docs/research/external-workflow-live.log` (8/8).                                                                                                                                                                                                                                                                                                     |
| Configuration          | **Prototyped (p4)**                                                                                | `.woof/` and `~/.woof/` JSON files (`woof.json`, `roles/<name>.json`, `workflows/<name>.{mjs,js,ts}`), project → user → built-in precedence with per-field provenance, and `woof config show`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Produced: `src/config/{schema,read,discover,resolve,record,index}.ts`, the `test/config.process.test.ts` precedence/provenance matrix (§6a), `woof config show`. p5 makes the built-in catalog itself name-keyed (`src/workflows/catalog.ts`, D2) and adds `planner` as a third built-in role, so a two-workflow, three-role catalog needed no branch. Needed: role instructions/context files in configuration (still an explicit non-goal — see `v1-evidence.md`'s "Role instruction/context files" limit), artifact retention.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Worker providers       | Open — a capability boundary shipped, one provider live-checked                                    | The `RuntimeAdapter` interface (`openPane, startAgent, observe, waitFor, deliver, stop`) is the capability boundary (D1); `createHerdrCliRuntime` implements it over the Herdr CLI (`herdr agent`/`pane` JSON commands, never terminal reads or key sends), and `createScriptedRuntime` implements it in memory for tests (`herdr-woof/testing`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Produced: `test/herdr-adapter.process.test.ts` (fake-`herdr` fixture, real child process, spawn boundary and pane-ownership rules; p3 adds a bounded `agent_pane_busy` start retry for a just-split pane, budgeted within the caller's own `startAgent` timeout); live acceptance against a real `claude` agent through Herdr — probe plus 11/11 hard gates passing (`docs/research/runtime-adapter-live.log`, regenerated fresh against the HEAD it documents each verification round). Build-review's own live acceptance is now confirmed too (13/13, see the Scheduling row below). Needed: a second provider kind beyond `claude` to prove the boundary generalizes — see `v1-evidence.md`'s "A second agent kind" limit; not addressed this phase.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Artifact contract      | Open — p1 subset prototyped, p2 adds opt-in detection, p3 closes C2, verdict agreement closed (p5) | Immutable accepted versions with small parseable control metadata. Same-user write access to `accepted/` is unchanged (C2): `chmod`/`chflags` were rejected as friction, not a boundary, so the response is detection, not prevention.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Produced: the p1 subset — immutable accepted copy under `accepted/<stage>/visit-<n>/attempt-<m>/`, scoped to that attempt's own artifact directory (`test/submit.cli.test.ts`, `test/engine-paths.cli.test.ts`). p2 adds opt-in re-hashing: `readSnapshot(runDir, {verifyArtifacts: true})` / `woof run show --verify-artifacts` report `integrity.artifacts = {checked, altered}` via `acceptedCopyProblem` (`src/journal/accepted-copy.ts`). p3 closes the C2 downstream-verification requirement: before rendering any request that references an accepted artifact or check evidence, the scheduler re-hashes it against its journal record; a mismatch ends the run `failed{reason:"input_artifact_altered: ..."}` before any dispatch (`src/scheduler/driver.ts`; `test/scheduler.process.test.ts` "BR-006"). **p5 closes metadata/verdict agreement (D5, R9 cut not taken):** check 17b (`src/submission/submit.ts`) rejects `verdict_artifact_mismatch` when a stage's declared, first-line-anchored `artifactVerdictMarker` disagrees with the envelope's verdict — opt-in per stage, 10 cases in `test/artifact-verdict.process.test.ts`. Needed: per-stage **structural** artifact schemas — still open; a new contract surface, a new authoring burden and a new validation area, deferred past 0.1.0 (see `v1-evidence.md`'s corresponding limit). |
| Runtime observation    | **Prototyped (p2), consumed by the scheduler (p3)**                                                | Pull-based Herdr CLI adapter, no socket client (D1); runtime lifecycle stays an in-memory, freshness-stamped overlay over the snapshot, never a journal record (D3) — a Herdr sample is lossy by construction, and journaling every poll would make replay depend on sampling cadence and wall clock.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Produced: `test/herdr-adapter.process.test.ts` (spawn boundary, pane ownership, delivery-precondition mapping, all through a fake `herdr` fixture reached by absolute path, never PATH); live acceptance against a real `claude` agent through Herdr, probe plus 11/11 hard gates passing, observed `stateChangeSeq` values monotonic and consistent with a server-global counter across two verification rounds (`docs/research/runtime-adapter-live.log`). p3's scheduler consumes the same tracker (a two-tick `ready` settle with an equal `stateChangeSeq` before dispatch or format repair) but never uses `stateChangeSeq` as delivery evidence (D8) — that scope (p2's open R2) stays unresolved either way. p5's four new live logs gate on scheduler/observer agreement (gate 10) and runtime loss, not on an independent second confirmation of R1/R2/R4; **this row stays open, not silently**, and its evidence is unchanged by p5. Needed: a second runtime kind (see `v1-evidence.md`'s "A second agent kind" limit), and independent confirmation of R1/R2/R4 beyond the live gates already run.                                                                                                                                                                                                                                                |

### Scheduling (new, p3)

| Choice     | Status                                  | Recommendation                                                                                                                                                                                                                                                                                                                                                                                               | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Scheduling | **Prototyped (p3); live-verified (p5)** | A pure decision core (`decide(view) -> Action`, D1) over exactly the snapshot an external observer would read, plus an effectful driver that executes one action per tick and re-reads before deciding again; every repeating route (rounds, visits, attempts, format repairs, readiness, blocking, delivery, the run itself) carries its own bound and ends the run `exhausted` with the limit's name (D9). | Produced: `src/scheduler/core.ts` (pure `decide`), `src/scheduler/driver.ts` (effects). `test/unit/scheduler-core.test.ts` (decision tables, no I/O) and `test/scheduler.process.test.ts` (21+ real-process scenarios: identity continuity, artifact handoff, gate decisions on validated control data only, every limit at its boundary, blocking resolution, cancellation with no resurrection of late results, duplicate/stale handling, revision binding surviving a moving repository, plus repair-round regressions BR-001/BR-101/BR-103/BR-201 and the BR-003 fast-worker race). **Live-verified (p5, carry-over C5):** `docs/research/build-review-live.log` records 13/13 hard gates against real `claude` agents through Herdr, replacing the round-1 log that recorded `GATE 10 FAIL` and 12/13 (a live-agent-behavior finding, not a scheduler defect — resolved by LV-102, see `v1-evidence.md`'s checklist-injection observation). `docs/research/plan-build-review-live.log` independently confirms the same scheduler against a second, three-agent workflow: 13/13. |

### Reuse (new, p5)

| Choice | Status                 | Recommendation                                                                                                                                                                                      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------ | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reuse  | **Live-verified (p5)** | A new workflow — built-in or external, project-authored — needs no engine-side branch: the same catalog, admission, scheduler and submission code serve every definition by its own declared shape. | Produced: `plan-build-review` (built-in, three agents, a new stage and artifact contract) and `scribe` (external, one stage, `roundStage: null`, no `limitDefaults`, an agent id that is not its role) both admit and run to completion through the unmodified engine. Live: `docs/research/plan-build-review-live.log` (13/13) and `docs/research/external-workflow-live.log` (8/8, via `/woof:run --workflow scribe`, exercising the discovered-workflow admission path — no launcher pre-admission, the pane host writes any rejection to `outcome.json` and the launcher reads it back as its own exit code: 2 for an admission rejection, 3 for an infrastructure reason). Offline: `test/plan-build-review.cli.test.ts`, `test/external-workflow.process.test.ts`, and `test/boundaries.process.test.ts`/`test/unit/config-resolve.test.ts` proving no built-in stage or workflow name appears in `dist/scheduler/**` or in a configuration-resolution branch. |

Resolve these into a spec and bounded tickets. Reopen product direction only when
new evidence shows a real conflict. Avoid repeating the entire design discussion
to choose a filename or helper function.

## Sources

The [conversation](https://chatgpt.com/c/6aa71569-4e7c-83ed-b6d6-03992919a7c4)
provides the direction and final artifact correction. The
[project assessment](../research/project-assessment.md) identifies the stashed
draft and scaffold branch as separate historical inputs.
