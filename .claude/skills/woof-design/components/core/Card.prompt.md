Flat bordered panel with a small uppercase header; no shadow, no colored borders.

```jsx
<Card title="Counters" actions={<IconButton icon="refresh" label="Refresh" />}>…</Card>
```

`padding={0}` when the body is a Table.
