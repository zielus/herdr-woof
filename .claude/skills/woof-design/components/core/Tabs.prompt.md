Underline tabs for switching views within a panel (Overview / Journal / Artifacts).

```jsx
<Tabs tabs={[{id:"journal",label:"Journal",count:42},"Artifacts"]} value="journal" />
```

Counts render as Badge.
