Action button; one primary per view, secondary for the rest, ghost in toolbars, danger only for "Cancel run".

```jsx
<Button variant="primary" icon="play">Start run</Button>
<Button variant="danger">Cancel run</Button>
```

Sizes sm 24px, md 28px, lg 36px. Labels are sentence case. `icon` takes an Icon name.
