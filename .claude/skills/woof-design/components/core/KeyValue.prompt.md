Definition list for run metadata (workflow, opened, owner, limits).

```jsx
<KeyValue items={[{k:"Workflow",v:"plan-build-review@1"},{k:"Owner",v:<Chip state="alive"/>}]} />
```

Keys are sentence-case sans, values mono.
