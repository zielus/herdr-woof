Status chip that colors an engine state word (kept lowercase, mono).

```jsx
<Chip state="running" />  <Chip state="blocked" />  <Chip state="pass" />
```

Tones: pass/fail/blocked/lost/idle/working. `working` pulses by default. Never title-case the word.
