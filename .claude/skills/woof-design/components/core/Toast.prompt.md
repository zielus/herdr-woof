Notification for the two events the host notifies on: a new block and termination.

```jsx
<Toast tone="blocked" title="Run blocked" detail='startup_blocked · open claude in /repo once' />
```

Detail is mono and quotes the engine reason verbatim.
