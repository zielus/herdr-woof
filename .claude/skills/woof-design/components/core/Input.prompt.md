Single-line text field. Use `mono` whenever the value is something the CLI would print.

```jsx
<Input mono icon="search" placeholder="Filter runs by id or repo" />
```

`label`/`hint` stack above/below; `invalid` turns the border and hint rose.
