Woof logo mark (symbol only) that picks the lavender or charcoal file by theme; optional "Woof" wordmark set in Geist.

```jsx
<Logo size={32} withName version="v0.1.0" base="../../assets/brand" />
```

`variant="tile"` renders the rounded tile. Never recolor; never below 32px except favicons.
