One journal record as a mono row; type colored by family (run.* accent, gate pass/reject, rejected rose, blocked amber).

```jsx
<JournalLine seq={12} ts="2026-09-16T20:41:07.221Z" type="gate.recorded" decision="reject" subject={{stageId:"review",visit:1,attempt:1}} data={{round:1}} />
```

Stack inside a Card with padding 0. Never reformat `data`; show the JSON.
