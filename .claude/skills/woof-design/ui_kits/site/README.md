# Plugin site UI kit (GitHub Pages)

A single-page site for the Woof plugin. The repo has no site today; content is lifted from `README.md`, `docs/integrations/plugins.md` and `herdr-plugin.toml`. Sections: nav, hero with a real CLI transcript, six "what it is" cards, install (Herdr / Claude Code / CLI tabs), commands table, exit-code grid, footer. Dark default, light toggle persisted.

Files: `index.html`, `Site.jsx`.
