---
description: Run a woof workflow (stub — not yet implemented)
argument-hint: "[<name>]"
allowed-tools: mcp__woof__*
---

Placeholder for running and steering a Woof workflow. The `woof` MCP server
exposes no tools yet, so this command has nothing to call.

When invoked, tell the user Woof's orchestration surface is not implemented
yet and stop — do not guess at a tool call or a workflow to run. The
arguments given were: `$ARGUMENTS`.
